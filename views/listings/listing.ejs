<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travelbase</title>
    <link rel="stylesheet" href="/listing.css">
</head>

<body>
    <form method="get" action="/listings/new">
        <button>add new</button>
    </form>
    <div class="lists">
        <% for( let listing of allList ) { %>


            <div class="list">

                <div class="imgs">
                    <% if (listing.image && listing.image.url) { %>
                        <img src="<%= listing.image.url %>" alt="<%= listing.image.filename %>">
                        <% } else { %>
                            <p><img src="https://plus.unsplash.com/premium_photo-1661915661139-5b6a4e4a6fcc?q=80&w=1267&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
                                    alt=""></p>
                            <% } %>
                </div>
                <p>
                    <a href="/listings/<%= listing._id %>">
                        <%= listing.title %>
                    </a>
                </p>

                <p>
                    price:<%= listing.price %>rs
                </p>
            </div>
            <% } %>
    </div>
</body>

</html>